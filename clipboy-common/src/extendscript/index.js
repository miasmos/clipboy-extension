import { evalJsxScript } from '@common/util';
export const {
    getVersionInfo,
    getProjectPath,
    importMedia,
    addTwitchMetaData,
    loadSettings,
    saveSettings,
    log,
    clearLog,
    getSep,
    getLocale
} = evalJsxScript;
