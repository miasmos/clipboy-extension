import { evalJsxScript } from '@common/util';
export const {
    getVersionInfo,
    getProjectPath,
    importMedia,
    addMediaMetaData,
    loadSettings,
    saveSettings,
    log,
    clearLog,
    getSep,
    getLocale
} = evalJsxScript;
