import { evalJsxScript } from '@common/util';
export const {
    getVersionInfo,
    getProjectPath,
    importTwitchClips,
    addTwitchMetaData,
    loadSettings,
    saveSettings,
    log,
    clearLog,
    getSep
} = evalJsxScript;
